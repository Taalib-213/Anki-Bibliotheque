[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Empêche que les fichiers .apkg soient mal interprétés
[[headers]]
  for = "/anki/*"
  [headers.values]
    Content-Type = "application/octet-stream"
    Content-Disposition = "attachment"
